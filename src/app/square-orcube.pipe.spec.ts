import { SquareORcubePipe } from './square-orcube.pipe';

describe('SquareORcubePipe', () => {
  it('create an instance', () => {
    const pipe = new SquareORcubePipe();
    expect(pipe).toBeTruthy();
  });
});
